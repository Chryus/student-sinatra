source 'https://rubygems.org'#same as :rubygems

gem 'sinatra', require: 'sinatra/base'

group :development do #run these only during development
	gem "shotgun" #allows us to not have to restart our server
	gem "tux" #allows us to have a console for our app, like a debugger for an if __FILE__ == $PROGRAM_NAME
	gem "debugger"
end 

# source "http://rubygems.org"

# gem 'sinatra'

# group :development do
#   gem 'tux'
#   gem 'shotgun'
#   gem 'debugger'
# end